// 設計書: フロントエンド設計(1) 認証・ダッシュボード
//
// ログイン後のトップページ（雛形）。
// 実装時はSupabase Authのセッションを確認し、未ログインならサインアップ/
// ログイン画面へリダイレクトすること。店舗データはJWTから取得した
// store_idに基づいて取得し、URLやクエリパラメータでstore_idを
// 受け取らないこと（テナント分離の原則）。

export default function DashboardPage() {
  return (
    <main>
      <h1>ダッシュボード（雛形）</h1>
      <p>
        ここに店舗ごとの概況（本日のおすすめメニュー、直近の生成コンテンツ、
        プラン情報など）を表示する。
      </p>
    </main>
  );
}
